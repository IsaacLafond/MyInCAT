datatable_options <- list(
    height = "100%",
    paging = FALSE,
    scrollX = TRUE,
    # scrollY = "300px",
    scrollY = "100%",
    scrollCollapse = TRUE
)

picker_input_options <- list(
    "live-search" = TRUE,
    "container" = "body",
    "dropup-auto" = FALSE,
    "dropdown-align-right" = "auto"
)